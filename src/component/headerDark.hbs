{{#with data}}
<div class="header_top">
    <div class="logo">
        <span class="logo_title">
            {{brandName}}
        </span>
    </div>
    <div class="nav">
        {{#each links}}
        <a href="{{url}}" class="nav_link">{{text}}</a>
        {{/each}}
    </div>
</div>
<div class="header_ad">
    {{#with ad}}
    <div class="header_ad_left">
        <div class="header_ad_logo">
            <span>Ad</span>
        </div>
        <a href="{{url}}" class="nav_link ad_link">{{url}}</a>
    </div>

    {{/with}}
    <span>Users online: {{userOnline}}</span>
</div>
{{/with}}